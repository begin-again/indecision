{
  "name": "indecision-app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "live-server public --browser=chromium-browser --port=3000",
    "compile": "babel src/app.js --out-file=public/scripts/app.js --presets=env,react --watch"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "semistandard": "^13.0.1"
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "1.5.2",
    "babel-preset-react": "6.24.1"
  },
  "eslintConfig": {
    "parserOptions": {
      "ecmaVersion": 6,
      "ecmaFeatures": {
        "jsx": true
      }
    }
  }
}
